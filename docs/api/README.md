# API docs (reserved)

This folder is empty on purpose until we start exporting OpenAPI snapshots.

Until then, the live contract is the FastAPI app (`services/api`, `/api/v1`) and the hand-written types in `packages/contracts`. Do not treat this directory as a second specification.
