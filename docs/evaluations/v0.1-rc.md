# v0.1 release-candidate verification (S104)

Recorded September 24, 2026 against `main` after S103 (`7832c1f`).

## Modes

| Mode | Result |
|---|---|
| AI off golden suite | 21 passed, 1 skipped |
| AI fake golden suite | 22 passed |
| API pytest | 189 passed, 6 skipped (AI off) |
| `make ai-eval` | Green with FakeProvider (S93 harness) |

## Performance (host)

API p95 (budget 250 ms), heavy fixture:

| Endpoint | p95 (ms) |
|---|---|
| home | 115.4 |
| progress | 161.8 |
| reviews/due | 85.6 |
| overview | 97.5 |
| session | 20.2 |
| plan-proposals | 44.0 |

Page LCP budget remains 2500 ms (Gate 4 harness). Container re-measure deferred to deploy host.

## AI evaluation

- FakeProvider: schema, safety tags, and fallback paths covered by `make ai-eval`.
- Live provider: not claimed in this environment (no production key). Record live p95 and fallback rates when a key is available; until then FakeProvider is the CI bar.
- `/metrics` exposes call counts, latency, and validator rejection when the gateway is on.

## Accessibility

- Axe: zero serious/critical on `/app` routes (baseline deleted at Gate 4).
- Keyboard: wizard, Studio, settings, sign-in, review snooze presets.
- Design-review checklist applied; see signed screens below.

## Design review — Gate 7 / RC screens

| Screen | Route | Notes |
|---|---|---|
| Landing | `/` | Brand + tagline; Sign in |
| Home | `/app` | One next action; goals; due count |
| Learn / Studio | `/app/learn/:id` | Sitting clock; tutor labeled when on |
| Review | `/app/review` | Fit line; snooze 3/24/72h |
| Progress | `/app/progress` | Evidence by goal; no % mastered |
| Settings | `/app/settings` | Prefs; export; delete |
| Wizard | `/app/goals/new` | 5 steps; presets; placement optional |
| Help / Privacy | `/app/help`, `/privacy` | Honest limits; retention window |

## Prove Loop demo v2

`docs/prove-loop-demo.md` including General-route outline path; tutor explanation when `AI_PROVIDER=fake`.

## Known limitations (v0.1)

- No Knowledge Vault, embeddings, or RAG.
- No code sandbox or project workspace.
- No `applied` evidence facet.
- Free recall / self-rating stay self-reported.
- AI drafts stay `provisional` until human review.
- Dev email token only in development; production needs managed OIDC + `AUTH_JWKS_URL`.
- Live-provider cost/latency not claimed without a recorded live run.

## P0 findings

None.
